[preferred]
# Hyprland handles screen capture; GTK supplies the Settings interface used
# for the standard light/dark preference. KDE keeps its native file picker.
default=hyprland;gtk;
org.freedesktop.impl.portal.Settings=gtk
org.freedesktop.impl.portal.FileChooser=kde;gtk;
